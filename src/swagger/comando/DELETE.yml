delete an comando based on the sent information
---
tags:
  - comando
parameters:
  - name: id
    in: path
    type: integer
    description: the id of the comando
  - name: id_sistema
    in: path
    type: integer
    description: the id of the sistema
responses:
  200:
    description: The comando was successfully deleted
    schema:
      example:
        comando:
          id: 1
          id_sistema: 1
